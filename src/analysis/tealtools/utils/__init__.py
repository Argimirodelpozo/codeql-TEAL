"""Utility helpers that aren't part of the core analysis pipeline.

Currently houses :mod:`tealtools.utils.chain` — the only network-touching
module in the library (fetches deployed approval programs off chain for
cross-contract auto-discovery and the behavioural-lift corpus).
"""
