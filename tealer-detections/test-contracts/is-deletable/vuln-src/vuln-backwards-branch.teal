#pragma version 8
// VULNERABLE: backwards branch — approves when OnCompletion == 5 (DeleteApplication)
// instead of rejecting. Intent was to reject delete, but bnz jumps to approve when equal.
txn OnCompletion
int 5
==
bnz approve   // BUG: when equal (DeleteApplication), we approve!
err
approve:
int 1
return
