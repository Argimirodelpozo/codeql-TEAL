#pragma version 10
// Caller: appcalls AppID 7000001 with selector "do_thing".
// The callee (AppID 7000001) is deletable — surfaced via the
// sec-guide cross-contract analysis as an is-deletable +
// unprotected-deletable finding on the callee.

itxn_begin
pushint 6
itxn_field TypeEnum
pushint 7000001
itxn_field ApplicationID
pushbytes "do_thing"
itxn_field ApplicationArgs
itxn_submit

pushint 1
return
