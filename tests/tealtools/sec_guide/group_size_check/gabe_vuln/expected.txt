Approval exit at prog.teal:41 is reachable without a global GroupSize check — attackers can pad the group with extra transactions.
Approval exit at prog.teal:87 is reachable without a global GroupSize check — attackers can pad the group with extra transactions.
