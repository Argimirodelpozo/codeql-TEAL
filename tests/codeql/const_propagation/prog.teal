#pragma version 10
// Constant flowing through identity-preserving ops (dup) plus
// arithmetic. mustValues / ConstantPropagation should track the
// literal across these.

pushint 42
dup
+
pushint 1
return
