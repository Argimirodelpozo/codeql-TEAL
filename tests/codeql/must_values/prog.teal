#pragma version 10
// Constant pushers should be captured by mustValues / const-tracking
// query. Tests that pushint and pushbytes are properly recognised
// with their literal values.

pushint 42
pop
pushbytes "hello"
pop
pushbytes 0xdeadbeef
pop

pushint 1
return
