#pragma version 10
// Loop on a runtime arg — iteration count unknown statically.
// Anything in or after the loop has cumulative = unbounded.

pushint 0
l_loop:
pushint 1
+
dup
txna ApplicationArgs 0
btoi
<
bnz l_loop

return
