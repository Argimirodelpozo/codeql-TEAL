#pragma version 10
// Caller: appcalls AppID 7000002 with selector "process".
// The callee approves without validating txn RekeyTo — surfaces
// rekey-to via the cross-contract sec-guide layer.

itxn_begin
pushint 6
itxn_field TypeEnum
pushint 7000002
itxn_field ApplicationID
pushbytes "process"
itxn_field ApplicationArgs
itxn_submit

pushint 1
return
