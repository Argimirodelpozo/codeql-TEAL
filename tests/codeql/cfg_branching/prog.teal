#pragma version 10
// Two-arm branch + join. CFG should have 4 BBs (entry/decider, cheap
// arm, expensive arm, join) — anything else means BB construction
// shifted under us.

txn ApplicationID
bnz l_b
pushint 1
b l_done

l_b:
pushint 2

l_done:
pushint 1
return
