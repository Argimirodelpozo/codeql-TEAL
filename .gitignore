# Heavy real-mainnet contract fixtures: kept out of git (the small curated
# fixtures under tests/tealtools + the puya corpus are tracked; these giants
# are local-only, regenerated on demand). Renamed from tests/dbs (CodeQL-era
# "database" naming) — there is no database, just .teal source.
tests/contracts/

# Editor / IDE / agent state
.claude/
.vscode/
.idea/

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.ipynb_checkpoints/

# Local lift dumps — scratch output from the IR/SSA/structure CLIs at top level
/*.puya.ir
/*.puya.opt.ir
/*.ssa.ir
/*.ssa.ir.txt

# Coverage artifacts
.coverage
.coverage.*
htmlcov/
coverage.xml
