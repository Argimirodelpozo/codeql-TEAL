#pragma version 10
// Delegated LogicSig that approves a fixed-amount payment but never
// constrains txn Lease -> the signed transaction can be replayed until
// the delegating key rotates. Should be flagged.
txn TypeEnum
int pay
==
assert
txn Amount
int 1000000
==
assert
int 1
return
