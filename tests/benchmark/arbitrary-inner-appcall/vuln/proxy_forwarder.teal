#pragma version 10
    txna ApplicationArgs 0
    byte 0x9a5b3c1d
    ==
    assert
    // forward(targetApp, methodArg): relay a call to a caller-named app.
    // Selector is checked; the TARGET app id is not -> call any app.
    itxn_begin
    int appl
    itxn_field TypeEnum
    txna ApplicationArgs 1
    btoi
    itxn_field ApplicationID
    txna ApplicationArgs 2
    itxn_field ApplicationArgs
    itxn_submit
    int 1
    return
