#pragma version 10
// VULN: sha256(AssetName) is deterministic — collision on the source
// implies collision on the hash.

txna ApplicationArgs 0
btoi
asset_params_get AssetName
assert                              // [name]
sha256                              // [sha256(name)]   ← still tainted
pushint 64
box_create
pop

pushint 1
return
