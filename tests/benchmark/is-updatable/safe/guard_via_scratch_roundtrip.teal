#pragma version 10
// SAFE: the OnCompletion guard is round-tripped through scratch before its
// assert. The path predicate lands on the LOAD output, so a consumer reading
// `value.defined_by` saw a `load` rather than the comparison — fixed by
// `common.resolve_through_copies`.
txn OnCompletion
int UpdateApplication
!=
store 0
load 0
assert
byte "k"
app_global_get
pop
int 1
return
