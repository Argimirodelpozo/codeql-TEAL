#pragma version 10
// Loop on a runtime arg — iteration count unknown statically.
// Lines in/after the loop cap at the program's budget ceiling.

pushint 0
l_loop:
pushint 1
+
dup
txna ApplicationArgs 0
btoi
<
bnz l_loop

return
