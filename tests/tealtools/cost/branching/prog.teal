#pragma version 10
// Two branches join. Cheap arm: just `pushint`. Expensive arm: keccak256.
// At the join, cumulative is the max — the keccak path's cost.

txna ApplicationArgs 0
btoi
bnz expensive

pushbytes "cheap"
b done

expensive:
pushbytes "data"
keccak256
pop

done:
pushint 1
return
