#pragma version 8
// T2: arithmetic on the constant side of the equality. The RHS is
// `500 + 500`, which tryAsIntDef resolves to 1000. The narrowing should
// still fire.

txn Fee
int 500
int 500
+
==
assert

txn Fee          // should narrow to 1000
pop
int 1
return
