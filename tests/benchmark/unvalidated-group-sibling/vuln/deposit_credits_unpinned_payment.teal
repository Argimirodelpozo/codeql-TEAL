#pragma version 10
    // deposit(): credit the caller for an accompanying payment (gtxn 0).
    // BUG: trusts gtxn 0 Amount without pinning gtxn 0 Receiver to this app,
    // so the attacker pays someone else and is still credited.
    txna ApplicationArgs 0
    byte 0xa1b2c3d4
    ==
    assert
    gtxn 0 Amount
    int 1000000
    >=
    assert
    txn Sender
    gtxn 0 Amount
    app_global_put
    int 1
    return
