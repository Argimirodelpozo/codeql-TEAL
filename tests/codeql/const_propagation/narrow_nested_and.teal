#pragma version 8
// T2: nested `&&` — three cmps composed. Post-assert, all three fields
// should narrow. The recursion through AND in guardDefAssertsEquality
// has to unwrap two levels of AND to reach each leaf.

txn Fee
int 1000
==

txn OnCompletion
int 0
==

&&

txn TypeEnum
int 1
==

&&

assert

// All three should narrow:
//   txn Fee → 1000
//   txn OnCompletion → 0
//   txn TypeEnum → 1
txn Fee
txn OnCompletion
+
txn TypeEnum
+
return
