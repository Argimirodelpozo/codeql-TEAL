#pragma version 8
// Evasion attempt: use computed constant (2+3) instead of literal int 5.
// We correctly reject DeleteApplication, but tryAsInt() only handles IntegerConstant
// — the detector may not recognize this as an OnCompletion==5 guard.
int 2
int 3
+
txn OnCompletion
==
bnz reject
int 1
return
reject:
err
