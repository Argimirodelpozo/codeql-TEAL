#pragma version 10
// Mixed scalar + array itxn fields. Locks in the row schema that
// `innerTxnFields.ql` produces — exact field names, line numbers,
// def kinds, and start/end-kind tags as the python-analysis layer
// reads them.

itxn_begin
pushint 6
itxn_field TypeEnum
pushint 1234567
itxn_field ApplicationID
pushbytes "do_thing"
itxn_field ApplicationArgs
itxn_submit

pushint 1
return
