"""Internal helpers outside the core analysis pipeline: :mod:`.chain` (the only
network-touching module), :mod:`.dot` (Graphviz primitives), :mod:`.serialize`
(JSON helpers), :mod:`.targets` (CLI target resolution).
"""
